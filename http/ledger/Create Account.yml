info:
  name: Create Account
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/accounts
  body:
    type: json
    data: |-
      {
        "name":"Jhon Dow"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
